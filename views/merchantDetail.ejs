<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Merchant Detail</title>
</head>
<body style="margin: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: linear-gradient(120deg, #f8fbff 0%, #eef3ff 100%); color: #1e293b;">
  <%- include('header') %>

  <main style="padding: 24px; max-width: 980px; margin: 24px auto; background: #ffffff; border: 1px solid #dbeafe; border-radius: 14px; box-shadow: 0 10px 25px rgba(30, 64, 175, 0.08);">
    <h2 style="margin: 0 0 12px; font-size: 30px; color: #0f172a;">Merchant Detail</h2>
    <p style="margin: 0 0 6px;"><strong>Name:</strong> <%= merchant.name %></p>
    <p style="margin: 0 0 14px;"><strong>Category:</strong> <%= merchant.category %></p>

    <h3 style="margin: 0 0 10px; color: #0f172a;">Transactions</h3>
    <% if (merchant.Transactions && merchant.Transactions.length) { %>
      <table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse; border-color: #cbd5e1;">
        <tr>
          <th style="background: #eff6ff;">ID</th>
          <th style="background: #eff6ff;">User</th>
          <th style="background: #eff6ff;">Amount</th>
          <th style="background: #eff6ff;">Quantity</th>
        </tr>
        <% merchant.Transactions.forEach((el) => { %>
          <tr>
            <td><%= el.id %></td>
            <td>
              <%= el.Wallet && el.Wallet.User ? el.Wallet.User.name : '-' %>
            </td>
            <td><%= el.formattedAmount %></td>
            <td><%= el.quantity %></td>
          </tr>
        <% }) %>
      </table>
    <% } else { %>
      <p style="color: #64748b;">Belum ada transaksi.</p>
    <% } %>
  </main>
</body>
</html>
