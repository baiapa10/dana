<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Merchant</title>
</head>
<body style="margin: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: linear-gradient(120deg, #f6fbff 0%, #eef3ff 100%); color: #1f2937;">
  <%- include('header') %>

  <main style="padding: 24px; max-width: 560px; margin: 28px auto; background: #ffffff; border: 1px solid #dbeafe; border-radius: 14px; box-shadow: 0 10px 25px rgba(30, 64, 175, 0.08);">
    <h2 style="margin: 0 0 18px; font-size: 28px; color: #0f172a;">Add Merchant</h2>

    <% if (errors && errors.length) { %>
      <ul style="margin: 0 0 16px; padding: 12px 16px; color: #b91c1c; background: #fef2f2; border: 1px solid #fecaca; border-radius: 10px;">
        <% errors.forEach((err) => { %>
          <li style="margin-bottom: 4px;"><%= err %></li>
        <% }) %>
      </ul>
    <% } %>

    <form action="/merchants/add" method="post">
      <div style="margin-bottom: 14px;">
        <label style="display: inline-block; margin-bottom: 6px; font-weight: 600;">Name</label><br>
        <input type="text" name="name" value="<%= oldInput.name || '' %>" style="width: 100%; box-sizing: border-box; border: 1px solid #cbd5e1; border-radius: 10px; padding: 10px 12px; font-size: 14px;">
      </div>

      <div style="margin-bottom: 14px;">
        <label style="display: inline-block; margin-bottom: 6px; font-weight: 600;">Category</label><br>
        <input type="text" name="category" value="<%= oldInput.category || '' %>" style="width: 100%; box-sizing: border-box; border: 1px solid #cbd5e1; border-radius: 10px; padding: 10px 12px; font-size: 14px;">
      </div>

      <div style="margin-bottom: 18px;">
        <label style="display: inline-block; margin-bottom: 6px; font-weight: 600;">Harga Satuan</label><br>
        <input type="number" name="price" min="1" value="<%= oldInput.price || '' %>" style="width: 100%; box-sizing: border-box; border: 1px solid #cbd5e1; border-radius: 10px; padding: 10px 12px; font-size: 14px;">
      </div>

      <button type="submit" style="border: 0; border-radius: 10px; background: #1d4ed8; color: #ffffff; padding: 10px 16px; font-weight: 600; cursor: pointer;">Submit</button>
    </form>
  </main>
</body>
</html>
